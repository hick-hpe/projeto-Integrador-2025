services:
  django:
    build: ./devquiz/
    command: python manage.py runserver 0.0.0.0:8000
    container_name: django
    ports:
      - "8000:8000"
    volumes:
      - ./devquiz:/app

  # react:
  #   build: ./reactapp/
  #   container_name: react
  #   ports:
  #     - "5173:80"
  #   depends_on:
  #     - django
  #   volumes:
  #     - ./reactapp:/app
  