aluno = CustomUser.objects.get(id=1)
emblema_ini_mat = Emblema.objects.get(id=1)

emblema_aluno_ini_mat = EmblemaUser.objects.create(
    aluno=aluno,
    emblema=emblema_ini_mat
)

emblema_inter_mat = Emblema.objects.get(id=2)

emblema_aluno_inter_mat = EmblemaUser.objects.create(
    aluno=aluno,
    emblema=emblema_inter_mat
)
